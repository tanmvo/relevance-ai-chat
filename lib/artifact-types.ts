/**
 * Legacy artifact kind type. Kept for backward compatibility with existing
 * message types and editor code. Artifacts are removed in favor of itinerary.
 */
export type ArtifactKind = "text" | "code" | "image" | "sheet";
